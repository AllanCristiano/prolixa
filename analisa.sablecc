Package Prolixa;

Helpers
    letter = ['a'..'z'] | ['A'..'Z'] ;
    digit = ['0'..'9'] ;
    capital_letter = ['A'..'Z'] ;
    end_comman = ';' ;
    reserved_words = 'This is program' | 'That would be all.' 
    | 'Let us begin:' | 'unalterable;' | 'alterable.' | 'symbol' 
    | 'number' | ' answer' | 'yes' | 'no' | 'vector of' | 'matriz';

Tokens
    this_is_program = 'This is program' ;
    identifier = capital_letter (letter)* ;
    that_would_be_all = 'That would be all.' ;

    